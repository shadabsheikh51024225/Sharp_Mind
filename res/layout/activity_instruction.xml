<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/z"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".InstructionActivity" >

    <ScrollView
        android:id="@+id/scrollView1"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true" >

        <TextView
            android:id="@+id/txt_level"
            android:layout_width="match_parent"
            android:layout_height="1733dp"
            android:text="This game is Mind speed tester game. To Play this game user need to remember Words which will be show on screen after that user have to find that words form wordlist. Hint function provied help. if user forgot word than Hint function will help to user maximum 3 times in the one play."
            android:textColor="@android:color/white"
            android:textSize="40dp" />
    </ScrollView>

</RelativeLayout>
